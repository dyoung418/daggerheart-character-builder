// Extension of the card art files under data/card-art/. Kept in its own module so a deployment
// that ships its own art in another format can replace this one file instead of patching
// card-render.js — see README, "Bring your own card art".
export const CARD_ART_EXT = "png";
